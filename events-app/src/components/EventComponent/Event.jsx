import React from 'react'
import data from '../../core/data'
import { useSelector, useDispatch } from 'react-redux';

function Event(){
    const dispatch = useDispatch()
    // all events come with a date.
    // 1. many events fetch them event
//**
data.foreach((event) => {

})
//

   //in year
// in Month
// in 

  return (
    <div>
        


    </div>
  )
}

export default Event